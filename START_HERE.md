# 🎧 Как запустить приложение (всё по шагам)

Бэкенд (поиск музыки из VK) уже лежит в папке **`backend/`** — его клонировали и настроили за тебя. Осталось вписать токены и запустить.

## Шаг 0. Один раз установить всё

В терминале (в Cursor: **Terminal → New Terminal**) выполни **один раз**:

```bash
cd /Users/user/Desktop/telegram-music-miniapp
chmod +x scripts/*.sh
./scripts/setup.sh
```

Скрипт поставит зависимости для фронта и бэкенда. Если установка Python-пакетов оборвётся по таймауту — просто запусти `./scripts/setup.sh` ещё раз. Дальше нужны только токены и запуск.

---

## Шаг 1. Токены и MongoDB (без этого поиск не заработает)

### 1.1 Токен Telegram-бота
1. Открой в Telegram бота **@BotFather**.
2. Отправь команду **/newbot**, придумай имя и username бота.
3. Скопируй выданный **токен** (длинная строка вида `123456:ABCdef...`).

### 1.2 Токен VK (для поиска музыки)
1. Открой в браузере: **https://vkhost.github.io**
2. Выбери приложение **VK Admin**.
3. Войди в свой ВКонтакте и разреши доступ.
4. Скопируй выданный **токен**.

### 1.3 Вписать токены в проект
1. Открой файл **`backend/.env`** в редакторе.
2. Замени строки:
   - `BOT_TOKEN=your_telegram_bot_token_here` → вставь свой токен от BotFather.
   - `VK_TOKEN=your_vk_admin_token_here` → вставь свой токен от VK.

Сохрани файл.

### 1.4 MongoDB (база для бэкенда)
Бэкенд не запустится без MongoDB. Варианты:

**Вариант A — установить локально (macOS):**
```bash
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

**Вариант B — через Docker:**
```bash
docker run -d -p 27017:27017 --name mongo mongo
```

---

## Шаг 2. Запуск

Нужно держать открытыми **два терминала**.

### Терминал 1 — бэкенд (поиск и музыка)
```bash
cd /Users/user/Desktop/telegram-music-miniapp
./scripts/start-backend.sh
```
Дождись сообщения вроде: `Uvicorn running on http://0.0.0.0:8000`

### Терминал 2 — фронт (сайт приложения)
```bash
cd /Users/user/Desktop/telegram-music-miniapp
./scripts/start-frontend.sh
```
Дождись сообщения: `Local: http://127.0.0.1:5173/`

### Открыть приложение
В браузере открой: **http://127.0.0.1:5173**  
Или в Cursor: **Cmd+Shift+P** → **Simple Browser: Show** → введи `http://127.0.0.1:5173`

В поиске введи, например, «Макс Корж» или «Rammstein» — должны появиться треки из VK.

---

## Если что-то не работает

- **Поиск ничего не находит** — проверь, что в `backend/.env` указан правильный **VK_TOKEN** и бэкенд запущен (терминал 1).
- **Бэкенд не стартует: "MongoDB" / "connection refused"** — запусти MongoDB (см. шаг 1.4).
- **Бэкенд пишет про BOT_TOKEN / VK_TOKEN** — замени в `backend/.env` плейсхолдеры на свои токены и перезапусти `./scripts/start-backend.sh`.
